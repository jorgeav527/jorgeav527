{
    "scripts": {
        "build": "zola build && npx @tailwindcss/cli -i ./static/input.css -o ./docs/style.css --minify",
        "dev": "concurrently \"zola serve\" \"npx @tailwindcss/cli -i ./static/input.css -o ./static/style.css --watch\"",
        "test": "echo \"Error: no test specified\" && exit 1"
    },
    "devDependencies": {
        "@tailwindcss/typography": "^0.5.16",
        "concurrently": "^9.2.1",
        "tailwindcss": "^4.2.2"
    },
    "engines": {
        "node": ">=22.19.0"
    }
}
